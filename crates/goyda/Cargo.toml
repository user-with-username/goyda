[package]
name = "goyda"
version = "0.1.0"
edition = "2024"

[dependencies]
jni = { version = "0.21.1", features = ["invocation"] } 
goyda-macros = { path = "../goyda-macros" }
once_cell = "1.21.4"
inventory = "0.3.15"
paste = "1.0"
goyda-derive = { path = "../goyda-derive" }

[features]
default = []
android = []
