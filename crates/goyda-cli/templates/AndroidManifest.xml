<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" 
    package="{package_name}">
    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="33" />
    <application android:label="{app_name}" android:hasCode="true" android:debuggable="{debuggable}" android:theme="@android:style/Theme.DeviceDefault">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".HotReloadSwapReceiver" android:exported="{debuggable}">
            <intent-filter>
                <action android:name="{package_name}.HOT_SWAP" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
